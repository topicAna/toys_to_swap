import { EventEmitter, AfterViewChecked } from '@angular/core';
import { Router } from '@angular/router';
import { SimplePageScrollService } from './ng2-simple-page-scroll.service';
export declare class SimplePageScroll implements AfterViewChecked {
    private router;
    private simplePageScrollService;
    private routerLink;
    private href;
    private pageScrollOffset;
    pageScrollFinish: EventEmitter<boolean>;
    private shouldScroll;
    constructor(router: Router, simplePageScrollService: SimplePageScrollService);
    handleClick(clickEvent: Event): boolean;
    private scrollAndEmitEvent();
    ngAfterViewChecked(): void;
}
